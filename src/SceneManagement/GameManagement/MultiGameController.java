package SceneManagement.GameManagement;

public class MultiGameController {
    // Properties

    // Constructor
    /*public MultiGameController(Stage stage, ArrayList<Player> players) {
        root = FXMLLoader.load(getClass().getResource("/UI/Game.fxml"));
        scene = new Scene(root, Color.BLACK);
        this.players = players;
        initialize(stage);
    }
*/
    // Methods
}
